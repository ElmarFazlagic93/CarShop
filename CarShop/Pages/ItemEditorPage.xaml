<?xml version="1.0" encoding="utf-8" ?>
<pages:BasePage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CarShop.Pages.ItemEditorPage"
             xmlns:pages="clr-namespace:CarShop.Pages"
             xmlns:viewmodels="clr-namespace:CarShop.ViewModels"
             x:DataType="viewmodels:ItemEditorPageViewModel"
             Title="{Binding Title}">
    <VerticalStackLayout Padding="20" Spacing="10">

        <Label Text="Name" />
        <Entry Text="{Binding Name}" Placeholder="Enter item name" />

        <Label Text="Description" />
        <Editor Text="{Binding Description}" Placeholder="Enter description" AutoSize="TextChanges" />

        <Label Text="Price" />
        <Entry Text="{Binding PriceString}" Keyboard="Numeric" />

        <Button Text="💾 Save"
                Command="{Binding SaveCommand}" />

        <Button Text="❌ Cancel"
                Command="{Binding CancelCommand}" />
    </VerticalStackLayout>
</pages:BasePage>